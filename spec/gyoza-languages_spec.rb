# frozen_string_literal: true

RSpec.describe GyozaLanguages do
  it 'has a version number' do
    expect(GyozaLanguages::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
